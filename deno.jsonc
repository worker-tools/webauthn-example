{
  "importMap": "./import-map.json",
  "tasks": {
    "serve": "export DEFAULT_KV_URL=sqlite://./webauthn.sqlite && deno run -A --location=http://localhost:8000 ./deno.ts",
    "dev": "export DEFAULT_KV_URL=sqlite://./webauthn.sqlite && deno run -A --location=http://localhost:8000 --watch --no-check ./deno.ts"
  }
}